import * as Styled from './index.styled';

const ProfileMenu = () => {
	return (
		<Styled.ProfileMenu>
			<ul>
				<li>Profile</li>
				<li>Log Out</li>
			</ul>
		</Styled.ProfileMenu>
	);
};

export default ProfileMenu;
